name: model-test
on: push

jobs:
  olivaw-test:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
    - uses: Wimmics/olivaw/test-actions@a90b717dec1a85d9ce51a63537deaf04a626306e
      with:
        gist-secret: ${{ secrets.GIST_SECRET }}